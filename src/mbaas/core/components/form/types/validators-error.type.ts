export enum ErrorTypes {
  required = 'required',
  requiredTrue = 'required',
  email = 'pattern',
  nullValue = 'null',
  onlyNumbers = 'pattern',
  maxNumber = 'max',
  minNumber = 'min',
  maxLength = 'maxlength',
  minLength = 'minlength',
  maxDate = 'maxDate',
  minDate = 'minDate',
  celPhone = 'celPhone',
  invalidDate = 'invalid',
  pattern = 'pattern',
  select = 'select'
}
